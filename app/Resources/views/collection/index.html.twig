{% extends 'base.html.twig' %}

{% block body %}
    <h1>Collections list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Shared</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for collection in collections %}
            <tr>
                <td>{{ collection.id }}</td>
                <td><a href="{{ path('collection_show', { 'id': collection.id }) }}">{{ collection.name }}</a></td>
                <td>{% if collection.shared %}Yes{% else %}No{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('collection_show', { 'id': collection.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('collection_new') }}">Create a new collection</a>
        </li>
    </ul>
{% endblock %}
